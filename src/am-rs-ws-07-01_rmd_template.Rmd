---
title: "W07-1: Data bundling (Landsat 7)"
output: html_document
---

In order to speed things up a little, here is a template for a markdown file
which can be used as part of this worksheet.

```{r, echo=FALSE, warning=FALSE, message=FALSE, results="hide"}
library(raster)
library(rgdal)
library(sp)

inpath_l7 <- "D:/active/moc/am-remote-sensing/examples/data/landsat/l7_2001-07-30/"
```

### Reading Landsat 7 datasets selected for land-cover classification
```{r}
# Your code here
landsat_7_pan <- ...

landsat_7_filter <- ...
```

### Save the bundled Landsat 7 data
```{r}
writeRaster(stack(landsat_7_pan, landsat_7_filter), 
            filename = paste0(inpath_l7, "l7_2001-07-30_30m_crop_pan_lc_bundle.tif"),
                              format = "GTiff", overwrite = TRUE)
```
